version: 1

project:
  exports:
  - format: typst
  - format: pdf
    template: arxiv_two_column
  # plugins:
  # - ./github-issues.mjs # might need https link

site:
  template: book-theme
  options:
    hide_toc: true
