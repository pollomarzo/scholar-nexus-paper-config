version: 1

project:
  # thumbnail fixed location is used by paper-gallery plugin
  thumbnail: thumbnails/thumbnail.png
  exports:
  - format: typst
    id: typst-pdf
  downloads:
  - id: typst-pdf
    title: PDF (Typst template)
  # plugins:
  # - ./github-issues.mjs # might need https link

site:
  template: book-theme
  options:
    hide_toc: true
